{
  "name": "photobooth",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "scripts": {
    "build": "npm install && npm run build -w frontend && npm run build -w backend",    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "dev": "concurrently -n \"BACK,FRONT\" -c \"cyan,magenta\" \"npm run dev:backend\" \"sleep 12 && npm run dev:frontend\""
  }
}